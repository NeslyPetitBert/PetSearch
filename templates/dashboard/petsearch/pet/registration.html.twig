{% extends "/layouts/dashboard_layout.html.twig" %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block title %}Ajouter un nouvel animal{% endblock %}

{% block body %}
<!-- Begin Page Content -->
    <div class="container-fluid">

        <div class="row">
            <div class="col-lg-6">

                <!-- Dropdown Card Example -->
                <div class="card shadow mb-4">
                    <!-- Card Header - Dropdown -->
                    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                        <h6 class="m-0 font-weight-bold text-primary">Ajouter un nouvel animal</h6>
                    </div>
                    <!-- Card Body -->
                    <div class="card-body">
                        <!-- Card Body -->
                        {{ form_start(form) }}

                            <div class="wrap-input100">
                                {{ form_row(form.name) }}
                            </div>
                            <div class="wrap-input100">
                                {{ form_row(form.type) }}
                            </div>
                            <div class="wrap-input100">
                                {{ form_row(form.race) }}
                            </div>
                            <div class="wrap-input100">
                                {{ form_row(form.birthday) }}
                            </div>
                            <div class="wrap-input100">
                                {{ form_row(form.sexe) }}
                            </div>
                            <div class="wrap-input100">
                                {{ form_row(form.dateacquisition) }}
                            </div>
                            <div class="wrap-input100">
                                {{ form_row(form.userIduser) }}
                            </div>
                            <div class="form-group">
                                {{ form_rest(form) }}
                            </div>
                            <button type="submit" class="btn btn-success mr-2">Créer</button>
                            <a href="{{ path('pets_index') }}" class="btn btn-primary">
                                Revenir à la liste des animaux
                            </a>
                            <button class="btn btn-light" type="reset">Annuler</button>
                            </form>
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>

        </div>

    </div>
    <!-- /.container-fluid -->
{% endblock %}
